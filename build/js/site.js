const menuButton=document.querySelector('.menu-button'),nav=document.querySelector('.primary-nav');menuButton.addEventListener('click',()=>{const open=menuButton.getAttribute('aria-expanded')==='true';menuButton.setAttribute('aria-expanded',String(!open));menuButton.setAttribute('aria-label',open?'Open navigation':'Close navigation');nav.classList.toggle('open');document.body.classList.toggle('menu-open',!open)});nav.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>{nav.classList.remove('open');menuButton.setAttribute('aria-expanded','false');document.body.classList.remove('menu-open')}));const languageButton=document.querySelector('.language-button');let language=localStorage.getItem('sts-language')||'en';function setLanguage(next){language=next;document.documentElement.lang=next==='hi'?'hi':'en';document.querySelectorAll('[data-en][data-hi]').forEach(el=>{el.textContent=el.dataset[next]});languageButton.children[0].classList.toggle('active',next==='en');languageButton.children[1].classList.toggle('active',next==='hi');languageButton.setAttribute('aria-label',next==='en'?'हिंदी में देखें':'View in English');localStorage.setItem('sts-language',next)}languageButton.addEventListener('click',()=>setLanguage(language==='en'?'hi':'en'));setLanguage(language);document.getElementById('year').textContent=new Date().getFullYear();const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
